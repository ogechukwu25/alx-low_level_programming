#include "main.h"


/**
 * _isupper - function that checks if a character is uppercase
 * @c: function parameter
 * Return: Always 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (i);
	}
	else
	{
		return (0);
